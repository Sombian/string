### TODO

- [ ] 100% testing coverage

### TODO/`fs`

- [x] `template<class S> file_of(const S& path)`

### TODO/`c_str`

- [ ] `template<class U, class X> ... split(const c_str<U, X>& value)`
- [ ] `template<class U, class X> ... find(const c_str<U, X>& value)`

### TODO/`slice`

- [ ] `template<class U, class X> ... split(const c_str<U, X>& value)`
- [ ] `template<class U, class X> ... find(const c_str<U, X>& value)`
